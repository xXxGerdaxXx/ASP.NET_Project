@model List<Infrastructure.Entities.ClientEntity>

@if (Model == null)
{
    <p>❌ Model is NULL!</p>
}
else if (!Model.Any())
{
    <p>⚠️ No clients available (empty list).</p>
}
else
{
    <div class="client-list">
        <table>
            <thead>
                <tr>
                    <th class="client-check"><input type="checkbox" /></th>
                    <th class="client-info">Client Name</th>
                    <th class="client-address">Address</th>
                    <th class="client-phone">Phone Number</th>
                    <th class="client-date">Created At</th>
                    <th class="client-actions"></th>
                </tr>
            </thead>
            <tbody id="clientTableBody">
                @foreach (var client in Model)
                {
                    <tr>
                        <td class="client-check"><input type="checkbox" /></td>
                        <td class="client-info">
                            <div class="client-name">@client.ClientName</div>
                            <div class="client-email">@client.Email</div>
                        </td>
                        <td class="client-address">@client.Address</td>
                        <td class="client-phone">@client.PhoneNumber</td>
                        <td class="client-date">@client.CreatedAt.ToString("yyyy-MM-dd")</td>
                        <td class="client-actions">
                            <i class="fa-solid fa-ellipsis" data-modal="true" data-target="#edit-client-modal"></i>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
