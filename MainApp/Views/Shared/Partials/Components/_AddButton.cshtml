@model string

@{
    // Generate a unique ID based on the button text
    var modalId = Model.Replace(" ", ""); // Example: "Add Project" → "AddProject"
}

<button class="btn btn-add" data-modal-target="add-project-modal">
    <i class="fa-solid fa-plus"></i> @Model
</button>
